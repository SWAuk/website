<?xml version="1.0" encoding="utf-8"?>
<form>
	<fieldset>

		<field name="id" type="text" default="0" label="Id"
			   readonly="true" class="readonly"
			   description="JGLOBAL_FIELD_ID_DESC"/>

		<field name="event_id" type="sql" default="1" key_field="id" value_field="name"
			   query="SELECT id, name FROM #__swa_event"

			   label="Event"


			   required="true"
			   filter="safehtml"/>

		<field name="ticket_type_id" type="sql" default="1" key_field="id" value_field="name"
			   query="SELECT id, name FROM #__swa_ticket_type"

			   label="Ticket type"


			   required="true"
			   filter="safehtml"/>

		<field name="quantity" type="text" size="40" class="inputbox"

			   label="Quantity"


			   required="true"
			   filter="safehtml"/>

		<field name="price" type="text" size="40" class="inputbox"

			   label="Price"


			   required="true"
			   filter="safehtml"/>

	</fieldset>

</form>
