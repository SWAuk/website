<?xml version="1.0" encoding="utf-8"?>
<form>
	<fieldset>

		<field name="id" type="text" default="0" label="Id"
			   readonly="true" class="readonly"
			   description="JGLOBAL_FIELD_ID_DESC"/>

		<field name="created_by" type="createdby" default=""
			   label="Created by"/>

		<field name="name" type="text" size="40" class="inputbox"

			   label="Name"


			   required="true"
			   filter="safehtml"/>

		<field name="university_id" type="sql" default="1" key_field="id" value_field="name"
			   query="SELECT id, name FROM #__swa_university WHERE `state` = 1"

			   label="University"


			   required="true"
			   filter="safehtml"/>

		<field name="season_id" type="sql" default="1" key_field="id" value_field="year"
			   query="SELECT id, year FROM #__swa_season WHERE `state` = 1"

			   label="Season"


			   required="true"
			   filter="safehtml"/>

		<field name="capacity" type="text" size="40" class="inputbox"

			   label="Capacity"


			   required="true"
			   filter="safehtml"/>

		<field name="date_open" type="calendar" class="inputbox"
			   required="true"

			   default="0000-00-00"

			   label="Date open"
			   filter="safehtml"/>

		<field name="date_close" type="calendar" class="inputbox"
			   required="true"

			   default="0000-00-00"

			   label="Date close"
			   filter="safehtml"/>

		<field name="date" type="calendar" class="inputbox"
			   required="true"

			   default="0000-00-00"

			   label="Date"
			   filter="safehtml"/>

		<field
				name="state"
				type="list"
				label="JSTATUS"
				description="JFIELD_PUBLISHED_DESC"
				class="inputbox"
				size="1"
				default="1">
			<option value="1">JPUBLISHED</option>
			<option value="0">JUNPUBLISHED</option>
		</field>

		<field name="checked_out" type="hidden" filter="unset"/>
		<field name="checked_out_time" type="hidden" filter="unset"/>

	</fieldset>

</form>
