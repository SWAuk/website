<?xml version="1.0" encoding="utf-8"?>
<form>
	<fieldset>

		<field name="id" type="text" default="0" label="Id"
			   readonly="true" class="readonly"
			   description="JGLOBAL_FIELD_ID_DESC"/>

		<!-- The below SQL will only select ids and name of MEMBERS! -->
		<field name="member_id" type="sql" default="1" key_field="id" value_field="name"
			query="SELECT m.id as id, u.name as name FROM #__swa_member as m
			INNER JOIN #__users AS u ON m.user_id=u.id"

			   label="User"


			   required="true"
			   filter="safehtml"/>

		<field name="competition_id" type="sql" default="1" key_field="id" value_field="event_competition_name"
			   query="SELECT competition.id, CONCAT( event.name, ' - ' , competition_type.name ) as event_competition_name FROM #__swa_competition AS competition
INNER JOIN #__swa_event AS event ON competition.event_id=event.id
INNER JOIN #__swa_competition_type AS competition_type ON competition.competition_type_id=competition_type.id"

			   label="Competition"


			   required="true"
			   filter="safehtml"/>

		<field name="result" type="text" size="40" class="inputbox"

			   label="Result"


			   required="true"
			   filter="safehtml"/>

	</fieldset>

</form>
